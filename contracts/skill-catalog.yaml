schema_version: 1
catalog_revision: skill-catalog-1
skills:
- skill_id: afds-doc-writer
  version: 1.4.0
  manifest_path: skills/afds-doc-writer/manifest.yaml
  skill_entrypoint: skills/afds-doc-writer/SKILL.md
  capabilities: [governed-document-authoring, document-structure-validation]
  use_when: [authoring or validating governed AFDS documents]
  do_not_use_when: [the task is only README, changelog, CI, or MCP architecture]
  loading: {modes: [runtime_tool, preload, vendored]}
- skill_id: agents-md-architect
  version: 1.4.0
  manifest_path: skills/agents-md-architect/manifest.yaml
  skill_entrypoint: skills/agents-md-architect/SKILL.md
  capabilities: [agent-instruction-authoring, agent-instruction-audit]
  use_when: [creating, migrating, or auditing AGENTS.md instruction systems]
  do_not_use_when: [the task does not change or verify repository agent instructions]
  loading: {modes: [runtime_tool, preload, vendored]}
- skill_id: changelog-release-architect
  version: 1.4.0
  manifest_path: skills/changelog-release-architect/manifest.yaml
  skill_entrypoint: skills/changelog-release-architect/SKILL.md
  capabilities: [changelog-governance, release-version-governance]
  use_when: [preparing or validating changelog and release version semantics]
  do_not_use_when: [the task is CI implementation without release documentation semantics]
  loading: {modes: [runtime_tool, preload, vendored]}
- skill_id: ci-cd-architect
  version: 1.4.0
  manifest_path: skills/ci-cd-architect/manifest.yaml
  skill_entrypoint: skills/ci-cd-architect/SKILL.md
  capabilities: [ci-cd-governance, protected-release-workflow]
  use_when: [designing or auditing CI, evidence, trust, and release workflows]
  do_not_use_when: [the task is application behavior without build or delivery implications]
  loading: {modes: [runtime_tool, preload, vendored]}
- skill_id: mcp-server-architect
  version: 1.4.0
  manifest_path: skills/mcp-server-architect/manifest.yaml
  skill_entrypoint: skills/mcp-server-architect/SKILL.md
  capabilities: [mcp-server-design, mcp-server-generation, mcp-provider-compatibility]
  use_when: [building, migrating, or validating MCP servers and their public contracts]
  do_not_use_when: [the task only consumes an existing MCP server]
  loading: {modes: [runtime_tool, preload, vendored]}
- skill_id: mcp-server-consumer
  version: 1.4.0
  manifest_path: skills/mcp-server-consumer/manifest.yaml
  skill_entrypoint: skills/mcp-server-consumer/SKILL.md
  capabilities: [mcp-consumption, mcp-tool-selection, mcp-retry-reconciliation]
  use_when: [planning safe calls to existing MCP servers]
  do_not_use_when: [the task is implementing the MCP server itself]
  loading: {modes: [runtime_tool, preload, vendored]}
- skill_id: readme-architect
  version: 1.4.0
  manifest_path: skills/readme-architect/manifest.yaml
  skill_entrypoint: skills/readme-architect/SKILL.md
  capabilities: [readme-authoring, readme-audit]
  use_when: [creating or repairing evidence-based repository README documentation]
  do_not_use_when: [the task is governed long-form documentation outside README ownership]
  loading: {modes: [runtime_tool, preload, vendored]}
