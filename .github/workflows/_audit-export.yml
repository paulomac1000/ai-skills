name: Audit snapshot export

on:
  push:
    branches:
      - refactor/skills-cleanup
    paths:
      - .github/workflows/_audit-export.yml

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - name: Archive exact revision
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$GITHUB_SHA"
          git archive --format=tar.gz --output=ai-skills-${GITHUB_SHA}.tar.gz HEAD
          sha256sum ai-skills-${GITHUB_SHA}.tar.gz > snapshot.sha256
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v4.6.2
        with:
          name: ai-skills-snapshot-${{ github.sha }}
          path: |
            ai-skills-${{ github.sha }}.tar.gz
            snapshot.sha256
          if-no-files-found: error
          retention-days: 1
